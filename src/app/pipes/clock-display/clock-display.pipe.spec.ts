import { ClockDisplayPipe } from './clock-display.pipe';

describe('ClockDisplayPipe', () => {
  it('create an instance', () => {
    const pipe = new ClockDisplayPipe();
    expect(pipe).toBeTruthy();
  });
});
